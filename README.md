# SvelteKit and TailwindCSS

``` sh
cp .vimrc ~/.vimrc
```

// run PlugInstall in vimrc

``` sh
npm init svelte@next
npx svelte-add tailwindcss --jit

yarn
chmod +x hotfix.sh
./hotfix.sh

yarn dev
```
